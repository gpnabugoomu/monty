#include "monty.h"
/**
  *f_nop- function with nothing
  *@head: the stack head
  *@counter: the line number
  *Return: always (0)
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
