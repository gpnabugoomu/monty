#include "monty.h"

/**
 * f_stack - function to print the top of stack
 * @head: the stack head
 * @counter: the line number
 * Return: always (0)
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
